name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: install gcc-8
      working-directory: ./CI/github
      run: chmod +x ./install_googletest.sh && sudo ./install_gcc8.sh
      shell: bash
    - name: install google test suite
      working-directory: ./CI/github
      run: chmod +x ./install_googletest.sh && sudo ./install_googletest.sh
      shell: bash
    - name: create build directory
      run: mkdir -p build
    - name: build
      working-directory: ./build
      run: cmake .. && make