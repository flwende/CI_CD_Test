name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: crate build directory
      run: mkdir -p build
    - name: build
      run: cd build && cmake .. && make
